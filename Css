body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 20px; /* يمكنك تعديل هذا إذا تداخل مع الاسم أو التنقل لاحقاً */
    background-color: #f4f4f4; /* خلفية رمادية فاتحة */
    color: #333;
    text-align: center; /* لتوسيط المحتوى الذي سيتوسط الصفحة لاحقاً */
    position: relative; /* أساسي لعمل position: absolute; للعناصر الفرعية مثل الاسم */
    min-height: 100vh; /* تأكد أن الـ body يغطي كامل ارتفاع الشاشة */
}

/* **تنسيقات اسمك في أعلى اليسار (للمهمة 1)** */
#website-title {
    position: absolute; /* يجعل العنصر يخرج من تدفق المستند العادي */
    top: 20px;         /* يضعه على بعد 20 بكسل من الحافة العلوية */
    left: 20px;        /* يضعه على بعد 20 بكسل من الحافة اليسرى */
    font-size: 28px;   /* حجم خط مناسب */
    color: #0056b3;    /* لون أزرق داكن */
    margin: 0;         /* إزالة أي هوامش افتراضية لـ h1 */
    padding: 0;        /* إزالة أي مسافات داخلية افتراضية لـ h1 */
    font-weight: bold; /* لجعله بارزاً */
    text-transform: uppercase; /* يمكنك إزالة هذا إذا لم ترغب في الأحرف الكبيرة */
}

/* هنا ستأتي أكواد CSS للمهام القادمة (مثل شريط التنقل، قسم About Me، إلخ).
   الأكواد الافتراضية لـ h1, h2, small-image وغيرها لم تعد موجودة لأننا بدأنا من الصفر.
*/
